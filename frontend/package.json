{
  "name": "kanglei-frontend",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "tailwindcss -i ./src/css/input.css -o ./dist/css/style.css --watch",
    "clean": "rm -rf dist",
    "build:css": "mkdir -p dist/css && tailwindcss -i ./src/css/input.css -o ./dist/css/style.css --minify",
    "build:static": "mkdir -p dist && cp -r src/*.html dist/ && cp -r src/admin dist/ && cp -r src/js dist/ && cp -r src/assets dist/ && cp -r src/img dist/",
    "build": "npm run clean && npm run build:static && npm run build:css",
    "start": "python3 -m http.server 5173 --directory dist"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.20",
    "postcss": "^8.4.47",
    "tailwindcss": "^3.4.17"
  }
}